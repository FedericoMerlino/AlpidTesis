@model IEnumerable<Alpid.Models.Caja>

@{
    ViewData["Title"] = "Caja";
}
<html>
<head>

</head>
<body>
    <header>
        <link href="~/css/cssKanze/style.min.css" rel="stylesheet" />
    </header>
    <h1>Caja Alpid</h1>
    <div class="form-group">
        <p>
            <a asp-action="CreateIngreso" class="btn btn-primary">Ingresar Dinero</a>
            @if (@ViewData["ValorParaBoton"].Equals(0))
            {
                <a asp-action="CreateRetire" class="btn btn-danger" style="display:none;">Retirar Dinero</a>
            }
            else
            {
                <a asp-action="CreateRetire" class="btn btn-danger">Retirar Dinero</a>
            }
        </p>
    </div>
    <div>
        <a asp-action="ViewAsPDFCaja" class="btn btn-dark" style="float:right;">Reporte caja</a>
    </div>
    <!--Buscar por fechas-->
    <form asp-action="Index" method="get">
        <div class="form-actions no-color">
            <p>
                Fecha desde<input type="text" name="fechaDesde" id="ponerFechaDesde" />
                Fecha hasta<input type="text" name="fechaHasta" id="ponerFechaHasta" />
                <input type="submit" value="Buscar" class="btn btn-dark" />

            </p>
        </div>
    </form>
    <!--Busca solamente la info que se muestra en la grilla-->
    <div>
        <i class="fa fa-search fa-2x" style="Float: right; padding-left:10px; padding-top:2px;"></i>
        <input id="searchTerm" type="text" onkeyup="doSearch()" class="form-control" placeholder="Buscar" style=" width:200px; Float: right; " />
    </div>
    <!--tabla con la informacion de la caja-->
    <div>
        <form class="form-group">
            <table class="table table-responsive-sm" id="datos">
                <thead>
                    <tr>
                        <th>
                            Fecha Movimiento
                        </th>
                        <th>
                            Observaciones
                        </th>
                        <th>
                            Ingreso
                        </th>
                        <th>
                            Salida
                        </th>
                        <th>
                            Total
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.FechaMovimiento)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Observaciones)
                            </td>
                            <td style="color:#49d292;">
                                @if (item.Debe != null)
                                {
                                    <label>$</label> @Html.DisplayFor(modelItem => item.Debe)
                                }
                            </td>
                            <td style="color:red;">
                                <!--if para que solamente traiga el signo de pregunta a los campos que tiene valores-->
                                @if (item.Haber != null)
                                {
                                    <label>$</label> @Html.DisplayFor(modelItem => item.Haber)
                                }
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Total)
                            </td>
                            <td>
                                <a asp-action="Details" asp-route-id="@item.CajaId">Ver</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </form>
    </div>
</body>
</html>
<script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
<link rel="stylesheet" type="text/css" href="dist/sweetalert.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.1/jquery-ui.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.1/jquery-ui.min.css" />


<script>
   
    $('#ponerFechaDesde').datepicker({
        format: "dd-mm-yyyy",
        language: "es",
    });
    $('#ponerFechaHasta').datepicker({
        format: "dd/mm/yyyy",
        language: "es",
    });
    document.getElementById('ponerFechaDesde').value = '@ViewData["FechaHastaFilter"]';
    document.getElementById('ponerFechaHasta').value = '@ViewData["FechaDesdeFilter"]';

    var valor = "@ViewData["Message"]";
    //si funciona todo correcto
    if (valor == 1) {
        swal({
            title: "Felicidades!!",
            text: "La operación se realizo con éxito",
            icon: "success",
            button: "Aceptar",
        });
    } else {
        //si se produce algún error
        if (valor == 2) {
            swal({
                title: "Error!!",
                text: "No se pudo realizar la operación",
                icon: "error",
                button: "Aceptar",
            });
        };
    };
</script>